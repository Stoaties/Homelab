# ArgoCD Installation
# This kustomization deploys ArgoCD to the cluster

apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: argocd

resources:
  - namespace.yaml
  - https://raw.githubusercontent.com/argoproj/argo-cd/stable/manifests/install.yaml

# Optional: Patch ArgoCD server to use LoadBalancer or NodePort
# Uncomment if you want external access
# patches:
#   - patch: |-
#       - op: replace
#         path: /spec/type
#         value: LoadBalancer
#     target:
#       kind: Service
#       name: argocd-server
